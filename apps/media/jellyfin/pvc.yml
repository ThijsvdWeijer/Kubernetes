apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-config
  namespace: media
spec:
  storageClassName: nfs-jellyfin
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
